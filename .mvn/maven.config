# Keep the repository build target compatible with the JDK used for this assessment.
# The parent POM currently declares JDK 25, while the local environment uses JDK 17.
-Djdk.version=17
-Dmaven.compiler.source=17
-Dmaven.compiler.target=17
